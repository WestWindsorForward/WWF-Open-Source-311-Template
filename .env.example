# Township Configuration
DOMAIN=localhost
ADMIN_EMAIL=admin@localhost

# Database
DB_USER=township
DB_PASSWORD=township
DB_NAME=township_db
DB_PORT=5432

# Redis
REDIS_PORT=6379

# Security - CHANGE IN PRODUCTION
SECRET_KEY=your-super-secret-key-change-me

# Initial Admin Account
INITIAL_ADMIN_USER=admin
INITIAL_ADMIN_EMAIL=admin@example.com
INITIAL_ADMIN_PASSWORD=admin123

# Emergency Access - CRITICAL: Change this to a strong random string in production!
# This provides admin backdoor access even when Auth0 is configured
# Generate with: openssl rand -base64 32
EMERGENCY_ACCESS_TOKEN=change-me-to-strong-random-token

# Google Cloud (Optional - for AI features)
GOOGLE_VERTEX_PROJECT=
GOOGLE_VERTEX_LOCATION=us-central1

# Frontend
FRONTEND_PORT=5173
