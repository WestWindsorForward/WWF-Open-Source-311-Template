# Core app settings
APP_ENV=production
SECRET_KEY=change-me-hex-32
JWT_SECRET=change-me-hex-32

# Public base URL for links in emails, PDFs, etc.
PUBLIC_BASE_URL=http://localhost

# Optional: Google Maps API key used by backend services (frontend uses Vite var)
GOOGLE_MAPS_API_KEY=

# Paths and storage
TOWNSHIP_CONFIG_PATH=config/township.yaml
UPLOADS_DIR=uploads

# Service URLs (Docker defaults work for Compose)
DATABASE_URL=postgresql+asyncpg://township:township@db:5432/township
REDIS_URL=redis://redis:6379/0

# Open311 integration (optional)
OPEN311_ENDPOINT_URL=
OPEN311_API_KEY=

# Email (choose Mailgun or SMTP)
MAILGUN_API_KEY=
MAILGUN_DOMAIN=
MAILGUN_FROM_EMAIL=

SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=

# SMS (Twilio optional)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_MESSAGING_SERVICE_SID=

# Vertex AI (optional)
GOOGLE_VERTEX_AI_ENDPOINT=
GOOGLE_VERTEX_AI_PROJECT=
GOOGLE_VERTEX_AI_LOCATION=
GOOGLE_VERTEX_AI_MODEL=
GOOGLE_APPLICATION_CREDENTIALS=

# Notes:
# 1) Copy this file to .env.local before running docker compose or ./setup.sh
#    cp .env.example .env.local
# 2) Generate secrets on Unix: openssl rand -hex 32
# 3) Frontend local dev uses apps/frontend/.env.local with VITE_* variables
