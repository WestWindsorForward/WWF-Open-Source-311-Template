# Frontend local development (Vite)
VITE_API_BASE=http://localhost:8000/api
VITE_GOOGLE_MAPS_API_KEY=

# Usage:
# 1) Copy to .env.local in apps/frontend for local dev: cp .env.example .env.local
# 2) When using Docker Compose, the frontend defaults to http://localhost/api via Caddy
