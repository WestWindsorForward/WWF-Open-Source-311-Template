apiVersion: apps/v1
kind: Deployment
metadata:
  name: township-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: township-redis
  template:
    metadata:
      labels:
        app: township-redis
    spec:
      containers:
        - name: redis
          image: redis:7
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: township-redis
spec:
  selector:
    app: township-redis
  ports:
    - port: 6379
      targetPort: 6379
